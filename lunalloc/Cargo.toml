[package]
name = "lunalloc"
version = "0.1.0"
edition = "2024"

[dependencies]


[target.'cfg(target_family = "windows")'.dependencies]
winapi = "0.3.9"

[target.'cfg(target_family = "unix")'.dependencies]
libc = "0.2.182"
